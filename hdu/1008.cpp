//Tue Aug 11 12:22:28 PM CST 2026
#include <bits/stdc++.h>
#define nl "\n"
using i64 = long long;
using i128 = __int128;
#define debug(x) std::cerr << #x << ": " << x << nl; 

void solve() {
    int n, w;
    std::cin >> n >> w;
    std::vector<char> op(n - 1);
    for (int i = 0; i < n - 1; ++i) {
        std::cin >> op[i];
    }
    std::cout << w << " ";
    for (int i = 0; i < n - 1; ++i) {
        
    }
}
int main() {
    std::ios::sync_with_stdio(false); 
    std::cin.tie(nullptr);
    int t;
    std::cin >> t;
    while (t--) solve();
}
